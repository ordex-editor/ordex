# Ordex Constitution

## Core Principles

### I. Minimal Dependencies
- Runtime dependencies are limited to 5 transitive crates.
- Dev-dependencies are limited to an additional 5 transitive crates.
- Never use dependencies with procedural macros or heavy build scripts.
- Prefer dependencies with no or few dependencies to keep room for future additions.

### II. Code Style
- Comments should explain intent and reasoning.
- ALL `unsafe` MUST be in its own file/module with a `// SAFETY:` comment above each block documenting all invariants and assumptions; no `unsafe` elsewhere, and if this is impossible, do not write the code.
- Default to the narrowest visibility possible for functions and methods: private first, then `pub(crate)`, and only `pub` when externally required.
- Prefer methods on types over free functions when the behavior logically belongs to the type.
- Never leave trailing whitespace at the end of lines.
- Before considering the task complete, you MUST run `cargo fmt` and `cargo clippy --all-targets --all-features -- -D warnings`. All formatting and lint issues must be fully resolved. Do not consider the task complete if any warning remains. This is a hard requirement.

### III. Testing
- In Rust, unit tests usually go in a `tests` sub-module of the module being tested.
- When fixing a bug: first create a regression test and commit it, then fix the bug in a new commit.

### IV. Project Structure
- Create the Rust project at the repository root (not in a subdirectory).
- Place all markdown files generated by agents (specs, tasks, reports, etc.) in a subdirectory, not at the project root.

### V. Documentation Maintenance
- When adding or updating features, update the corresponding user-facing documentation in the docs site in the same change.

### VI. Git Workflow
- All changes must be done in a feature branch.

## Governance
- This constitution supersedes all other practices.
- Amendments require documentation, approval, and a migration plan.

**Version**: 1.2.5 | **Ratified**: 2026-02-05 | **Last Amended**: 2026-03-10
